// tailwind.config.js
module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Ensure all relevant files are included
  ],
  theme: {
    extend: {
      // Extend as needed
    },
  },
  plugins: [],
};
